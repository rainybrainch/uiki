{
  "buildCommand": "npm install && cd packages/database && npx prisma generate && cd ../../apps/uwiki && npm run build",
  "outputDirectory": "apps/uwiki/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
